jQuery(document).ready(function($) {


  function showChannelsPage(params){
    // запрашиваем страницу каналов для заданного в параметрах тарифа
    // выводим страницу в заданный в параметрах контейнер
    var container = params.container;
    var tarif = params.tarif

  }


})